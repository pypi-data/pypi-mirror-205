[tool.poetry]
name = "via-hub-logistc"
version = "1.0.14"
description = "This project is intended to be a facilitator for new developments for test automation"
authors = ["Jaderson Macedo <jaderson.macedo@viavarejo.com.br>"]
readme = "README.md"
packages = [{include = "via_hub_logistc"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
