[tool.poetry]
name = "niceapp"
version = "0.0.0"
description = "Wrappers to make NiceGUI a single page app"
authors = ["CJ Lazell <cjlazell@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.7,<4.0"
nicegui = ">=1.2.9"

[tool.poetry.group.dev.dependencies]
pytest = "^7.3.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
