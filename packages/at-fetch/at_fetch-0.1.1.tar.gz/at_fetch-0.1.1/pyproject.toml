[tool.poetry]
name = "at_fetch"
version = "0.1.1"
description = ""
authors = ["thecatshidog"]
license = "MIT"
readme = "README.md"
packages = [{include = "fetch"}]

[tool.poetry.dependencies]
python = ">=3.7"
httpx = {extras = ["http2"], version = "^0.24.0"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
