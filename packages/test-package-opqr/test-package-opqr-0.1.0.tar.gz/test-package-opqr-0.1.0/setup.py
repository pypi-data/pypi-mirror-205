#!/usr/bin/env python3
from setuptools import setup

setup(
    name = "test-package-opqr",
    version = "0.1.0",
    py_modules = ["test_lib"],
    description = "A test package.",
    long_description = "A test package.",
)
