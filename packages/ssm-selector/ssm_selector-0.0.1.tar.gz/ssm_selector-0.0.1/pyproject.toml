[tool.poetry]
name = "ssm-selector"
version = "0.0.1"
description = "An interactive SSM Session starter'"
license = "MIT"
authors=["kkmiura"]

[tool.poetry.scripts]
ssm-selector = "ssm_selector.main:main"

[tool.poetry.dependencies]
python = "^3.8"
boto3 = "^1.26.117"
toml = "^0.10.2"

[tool.poetry.dev-dependencies]
black = "^23.3.0"
isort = "^5.12.0"
flake8 = "^6.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
